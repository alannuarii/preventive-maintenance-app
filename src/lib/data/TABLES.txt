CREATE TABLE jso (
    id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    unit INT NOT NULL,
    waktu DATE NOT NULL,
    service_hour INT NOT NULL,
    ADD CONSTRAINT unique_unit_waktu_service UNIQUE (unit, waktu, service_hour)
);
